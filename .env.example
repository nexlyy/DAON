# Point this at a reservation backend to replace the in-browser demo adapter.
# Leaving it unset keeps the site fully static, which is what GitHub Pages needs.
# VITE_BOOKING_API_URL=https://api.example.com/v1

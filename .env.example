# The reservation API — server/ in this repository. It stores the bookings and
# tells the restaurant about them over Telegram. Leave it unset and the site
# falls back to the in-browser demo adapter, which forgets everything on reload.
# VITE_BOOKING_API_URL=https://example.com/daon
